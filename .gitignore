# to add a file: git add -f

# ignore everything in the home directory
# files and folders starting with ! are NOT ignored
/*

!/.gitignore
!/.local
!/.config

!/.vimrc
!/.tmux.conf
!/.bashrc
!/.profile

/.local/share
