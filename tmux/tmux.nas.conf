source-file ~/.config/tmux/tmux.conf

# NAS-specific layout: single bottom status bar, no top pane border status line.
set -g status on
set -g status-position bottom
set -g status-right ""
set -g pane-border-status off

# Initialize new NAS sessions with three fixed windows.
set-hook -g after-new-session "rename-window -t 1 1 ; new-window -n 2 ; new-window -n 3 ; select-window -t 1"

# Reload NAS config (base + NAS overrides).
bind r source-file ~/.config/tmux/tmux.nas.conf \; display "Config reloaded!"
